<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.captainhumza.fypversion2">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_customer_home_two"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Customer.CustomerHomeTwoActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Rider.RiderHomeActivity"
            android:label="@string/title_activity_rider_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SignUpActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Customer.OrderdList"
            android:label="Order list"
            android:parentActivityName=".Customer.OrderdList" />
        <activity android:name=".Customer.AddToCartActivityCustomer" />
        <activity android:name=".Rider.WalletforRider" />
        <activity android:name=".ProfileDesign" />
        <activity android:name=".Rider.PaymentDetails" />
        <activity android:name=".Customer.WalletforCustomer" />
        <activity android:name=".Vendor.WalletforVendor" />
        <activity
            android:name=".Vendor.VendorHome"
            android:label="@string/title_activity_vendor_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MarkStores" />
        <activity android:name=".MapPlacePick" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Vendor.CustomerListforVendor" />
        <activity android:name=".Vendor.RiderDetailsforVendor" />
        <!-- <activity android:name=".Customer.CustomersFragments.AllProductListFragmentCustomer"></activity> -->
        <activity android:name=".Customer.MapDirections"></activity>
    </application>

</manifest>